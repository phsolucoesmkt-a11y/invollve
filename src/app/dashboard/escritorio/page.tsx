// The office itself is rendered persistently by the dashboard layout (OfficeShell).
// This route is the "home" state where no module window is open — so it renders nothing.
export default function EscritorioPage() {
  return null
}
